@AGENTS.md

## Claude-Specific Notes

- prefer the tracked root docs and `Doc/` files over any local-only `.agent/` material
- keep changes phase-scoped and update linked docs in the same slice
- when a generated ledger needs wording or path changes, update the generator and refresh the output instead of hand-maintaining drift
